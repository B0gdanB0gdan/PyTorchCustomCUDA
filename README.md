# Reinterpret the Linear layer from PyTorch

* CUDA C samples to learn the CUDA way of thinking
* Build the CUDA C implementation for forward and backward passes
* Make the code available in Python using Pybind
* Create a custom autograd.Function to allow for gradient tracking
* Compare custom implementation with official one
